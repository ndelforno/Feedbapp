<%= form_for([@request, @choice]) do |f| %>
<div class="choice">
  <%= f.label :choice %>
  <%= f.text_field :choice_text %>

  <%= f.submit 'Add choice', :class => "submit" %>
</div>

<%= link_to 'No more choices', requests_path, :class => "stop_choice" %>
<% end %>
